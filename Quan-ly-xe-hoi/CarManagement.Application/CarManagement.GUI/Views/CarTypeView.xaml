<UserControl x:Class="CarManagement.GUI.Views.CarTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarManagement.GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Car Type Management" Style="{StaticResource PageHeaderStyle}"/>
        <Border Grid.Row="1" Style="{StaticResource ContentCardStyle}" Margin="20,0">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="txtSearchCarType" Text="search car type..." Width="200" Style="{StaticResource InputStyle}" Margin="0,0,10,0" />
                <Button x:Name="btnCarTypeAdd" Content="Add +" Style="{StaticResource BtnSuccessStyle}" Margin="120,0,5,0" MinWidth="80"/>
                <Button x:Name="btnCarTypeUpdate" Content="Update" Style="{StaticResource BtnPrimaryStyle}" Margin="5,0,5,0" MinWidth="80"/>
                <Button x:Name="btnCarTypeDelete" Content="Delete" Style="{StaticResource BtnDangerStyle}" Margin="5,0,0,0" MinWidth="80"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource ContentCardStyle}" Margin="20,15,20,10">
            <DataGrid x:Name="dgCarTypes" Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                    <DataGridTextColumn Header="Type Name" Binding="{Binding Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>