<UserControl x:Class="CarManagement.GUI.Views.CarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarManagement.GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White" Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Car Management" Style="{StaticResource PageHeaderStyle}"/>

        <Border Grid.Row="1" Style="{StaticResource ContentCardStyle}" Margin="20,0">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="txtSearchCar" Width="200" Style="{StaticResource InputStyle}" Margin="0,0,10,0" TextChanged="txtSearchCar_TextChanged" />
                <ComboBox x:Name="cboCarFilter" Width="150" Style="{StaticResource ComboBoxInputStyle}" Margin="0,0,10,0" SelectionChanged="cboCarFilter_SelectionChanged">
                    <ComboBoxItem Content="All"/>
                    <ComboBoxItem Content="Car Model"/>
                    <ComboBoxItem Content="Brand"/>
                    <ComboBoxItem Content="Car Type"/>
                    <ComboBoxItem Content="Car Engine"/>
                </ComboBox>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0,0,0">
                    <Button x:Name="btnCarAdd" Content="Add" Style="{StaticResource BtnSuccessStyle}" Margin="50,0,5,0" MinWidth="80" Click="btnCarAdd_Click"/>
                    <Button x:Name="btnCarUpdate" Content="Update" Style="{StaticResource BtnPrimaryStyle}" Margin="5,0,5,0" MinWidth="80" Click="btnCarUpdate_Click"/>
                    <Button x:Name="btnCarDelete" Content="Delete" Style="{StaticResource BtnDangerStyle}" Margin="5,0,0,0" MinWidth="80" Click="btnCarDelete_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource ContentCardStyle}" Margin="20,15,20,10">
            <DataGrid x:Name="dgCars" Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding CarId}" Width="Auto"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding CarModel}" Width="*"/>
                    <DataGridTextColumn Header="Engine Type" Binding="{Binding CarEngine}" Width="*"/>
                    <DataGridTextColumn Header="Vehicle Type" Binding="{Binding CarType.typeModel}" Width="*"/>
                    <DataGridTextColumn Header="Number of Seats" Binding="{Binding CarSeat}" Width="*"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding CarDescription}" Width="*"/>
                    <DataGridTextColumn Header="Year of Publication" Binding="{Binding YearPublish}" Width="*"/>
                    <DataGridTextColumn Header="Price in Dollars" Binding="{Binding DollarPrice}" Width="*"/>
                    <DataGridTextColumn Header="Vehicle Warranty" Binding="{Binding CarWarranty}" Width="*"/>
                    <DataGridTextColumn Header="Brand" Binding="{Binding Brand.brandName}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
